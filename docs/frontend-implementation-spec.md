# Frontend Implementation Spec

## Purpose

프론트엔드는 관리자 화면과 사용자 화면의 성격이 다르므로 구현 방식을 분리한다.

- 관리자 화면: Bootstrap 기반
- 사용자 화면: Custom CSS 기반

Bootstrap은 버튼, 폼, 카드, 테이블, 모달, 그리드 등 일반적인 업무형 UI를 빠르게 구성하는 데 사용한다. 사용자 화면은 오마고치 캐릭터와 실습실 경험을 중심으로 하므로 게임형 UI를 위해 Custom CSS를 사용한다.

## Screen Implementation Criteria

| 구분 | 대상 화면 | 구현 방식 | 선정 이유 |
| --- | --- | --- | --- |
| 관리자 화면 | 로그인 | Bootstrap | 일반적인 입력 폼 구조로 빠른 구현 가능 |
| 관리자 화면 | 회원가입 | Bootstrap | 입력 필드, 버튼, 검증 메시지 구성에 적합 |
| 관리자 화면 | 비밀번호 변경 | Bootstrap | 로그인 화면과 동일한 폼 구조를 재사용하기 적합 |
| 관리자 화면 | 관리자 로그인 | Bootstrap | 사용자 화면과 구분되는 관리 UI 구성 가능 |
| 관리자 화면 | 관리자 대시보드 | Bootstrap Card / Grid | 출석률, 현재 인원, 학습 시간 등 통계 카드 구성에 적합 |
| 관리자 화면 | 출석 관리 테이블 | Bootstrap Table | 사용자별 출석 상태, 입실/퇴실 시간 조회에 적합 |
| 관리자 화면 | 방/회의실 생성 폼 | Bootstrap Form / Modal | 이름, 인원, 상태 입력 UI에 적합 |
| 관리자 화면 | 사용자 목록 | Bootstrap Table | 사용자 목록과 상태 관리에 적합 |
| 관리자 화면 | 통계 카드 | Bootstrap Card | 주요 지표를 카드 형태로 빠르게 구성 가능 |
| 사용자 화면 | 오마고치 홈 | Custom CSS | 캐릭터 중심의 게임형 UI 필요 |
| 사용자 화면 | 실습실 배치도 | Custom CSS | 책상, 의자 슬롯, 캐릭터 배치 표현 필요 |
| 사용자 화면 | 의자 슬롯 | Custom CSS Component | 출석/학습/부재중 상태를 시각적으로 구분 |
| 사용자 화면 | 캐릭터 표시 | GIF/PNG + CSS | 픽셀 캐릭터 및 상태 변화 표현 |
| 사용자 화면 | EXP 바 | Custom CSS | 게임식 성장 UI 표현 |
| 사용자 화면 | 말풍선 | Custom CSS | 캐릭터 피드백 표현 |
| 사용자 화면 | 뱃지 카드 | Custom CSS | 업적/보상 형태의 카드 표현 |
| 사용자 화면 | 퀘스트 카드 | Custom CSS | 할 일/일일 퀘스트 형태 표현 |
| 사용자 화면 | 학습 타이머 HUD | Custom CSS + JS | 게임 HUD처럼 학습 상태 표시 |

## Frontend Requirements

| ID | 항목 | 명세 |
| --- | --- | --- |
| FE-01 | 화면 구현 방식 분리 | 관리자 화면은 Bootstrap 기반, 사용자 화면은 Custom CSS 기반으로 구현한다. |
| FE-02 | 관리자 화면 | 로그인, 회원가입, 관리자 로그인, 대시보드, 테이블, 폼, 통계 카드는 Bootstrap 컴포넌트를 활용한다. |
| FE-03 | 사용자 화면 | 오마고치 홈, 실습실 배치도, 의자 슬롯, 캐릭터, EXP 바, 말풍선, 뱃지, 퀘스트, 타이머 HUD는 Custom CSS로 구성한다. |
| FE-04 | 실습실 배치도 | 책상은 정적 요소로 처리하고, 의자 슬롯은 사용자 상태를 표시하는 동적 요소로 처리한다. |
| FE-05 | 의자 슬롯 | 의자 슬롯은 빈 자리, 내 자리, 재실, 학습 중, 부재중, 회의중, 퇴실 상태를 시각적으로 구분할 수 있어야 한다. |
| FE-06 | 캐릭터 이미지 | 픽셀 이미지 확대 시 `image-rendering: pixelated` 속성을 적용한다. |
| FE-07 | 타이머 표시 | 프론트엔드는 학습 경과 시간을 표시하되, 실제 기록은 백엔드 서버 시간을 기준으로 저장한다. |
| FE-08 | 보상 피드백 | 출석, 퀘스트 완료, 학습 종료 시 EXP, 포인트, 스트릭 등의 피드백을 화면에 표시할 수 있어야 한다. |
| FE-09 | 반응형 처리 | 관리자 화면은 Bootstrap Grid를 활용하고, 사용자 화면은 기준 해상도에 맞춰 비율 기반 레이아웃을 적용한다. |
| FE-10 | 비밀번호 변경 화면 | 로그인 화면과 같은 UI 톤으로 아이디 확인 단계와 새 비밀번호 입력 단계를 분리한다. |
| FE-11 | 회원가입 중복 확인 | 로그인 ID 또는 이메일 중복 확인 결과를 사용자에게 표시할 수 있어야 한다. |
| FE-12 | 회원가입 추가 정보 | 생년월일은 날짜 입력으로 받고, 성별은 `female` / `male` 선택 버튼으로 입력받는다. |

## User UI Direction

- Figma는 최종 시안이 아니라 배치와 흐름을 설명하는 프로토타입으로 본다.
- 실제 구현은 현재 프로젝트의 픽셀/게임 UI 톤을 유지한다.
- 사용자의 주요 시선은 캐릭터, 오늘의 퀘스트, 실습실 이동 버튼에 먼저 닿아야 한다.
- 햄버거 메뉴와 실습실 이동 버튼은 현재 홈 화면 위치를 유지한다.
- CSS 추가 고도화는 현재 보류한다.

## Ranking Dashboard Requirements

| ID | 요구사항 | 구현 방향 |
| --- | --- | --- |
| RK-01 | 사용자는 출석률 기반 랭킹을 확인할 수 있어야 한다. | 출석 로그 기반 집계 |
| RK-02 | 사용자는 공부 시간 기반 랭킹을 확인할 수 있어야 한다. | `study_session.duration` 합산 |
| RK-03 | 랭킹은 일간, 주간 기준으로 조회할 수 있어야 한다. | `period` 파라미터 사용 |
| RK-04 | 전체 사용자 기준 랭킹을 제공할 수 있어야 한다. | 전체 사용자 기준 조회 |
| RK-05 | 팀 기준 랭킹을 제공할 수 있어야 한다. | `groupId` 파라미터 사용 |
| RK-06 | 사용자는 자신의 현재 순위를 확인할 수 있어야 한다. | `myRank` 또는 `isMe` 필드 제공 |
| RK-07 | 동일 점수 사용자는 공동 순위로 처리한다. | 공동 순위 처리 |
| RK-08 | 랭킹에는 실명 대신 닉네임 또는 표시명을 사용한다. | `nickname` 필드 사용 |
| RK-09 | 수동 기록은 타이머 기록과 구분하여 반영할 수 있어야 한다. | `recordType` 구분 |

## Attendance And Status UI Notes

- 출결 상태와 현재 상태는 UI에서 분리해서 표현한다.
- 출결 상태는 하루 단위 최종 결과이며 출석률, 랭킹, 관리자 통계에 사용한다.
- 현재 상태는 실시간 위치 또는 활동 상태이며 실습실 배치도, 타이머, 관리자 현황에 사용한다.
- 사용자 화면의 실습실 슬롯은 현재 상태를 우선 표시한다.
- 관리자 화면의 출석 관리 테이블은 출결 상태와 현재 상태를 함께 조회할 수 있어야 한다.
