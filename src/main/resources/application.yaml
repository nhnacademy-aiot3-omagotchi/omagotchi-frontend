server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: frontend

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
  endpoint:
    health:
      show-details: never
